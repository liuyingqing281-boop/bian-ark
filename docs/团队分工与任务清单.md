# 团队分工与任务清单

> 2026-08-17 定稿 ｜ 三人协作 ｜ 配套 PRD：`PRD-彼岸3.0.md`（产品定位）、`PRD-墓园2.0.md`（功能实现）
> 仓库：https://github.com/liuyingqing281-boop/bian-ark ｜ 协作约定：开工先 pull，收工 push；`dictionaries.ts` 各改各的段落

## 分工总览

| 成员 | 负责域 | 核心目标 |
|---|---|---|
| 刘洋 | 数字人模块（T11 收尾） | 视频生成真实可用、任务不丢、体验友好 |
| 颜新航 | 提示词生成模块（新） | LLM 地基 + 祭品提示词助手 |
| 刘贻清 | 资质合规 + 部署 + 统筹 | 卡上线的账号级操作、漏斗数据运营 |

## 刘洋 · 数字人模块

| # | 任务 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 视频模型开通 + 真实端到端验证 | 前置：方舟控制台开通 `doubao-seedance-2-5-260628`（或限时 4 折的 `doubao-seedance-2-0-mini-260615`，需同步改 `.env.local` 的 `ARK_VIDEO_MODEL`）。开通后跑通 提交→生成→审核→发布 全链路 | **P0** |
| 2 | 持久化任务 worker（T11.3 遗留） | 现为进程内轮询（`src/lib/digitalhuman.ts` 的 `pollArkTask`），服务重启任务永久卡 processing，只能等 60 分钟 `dh-worker` 超时兜底。改为任务状态表 + `tools/dh-worker.mjs` 定时扫描断点恢复 | **P0** |
| 3 | 长文稿支持 | 视频上限 5~12 秒但文稿允许 500 字。方案 A：按字数分段生成再拼接；方案 B：前端按估算时长做字数引导 | P1 |
| 4 | 错误提示友好化 | `ark_http_404: ModelNotOpen` 这类原始错误码映射为中文人话（`dictionaries.ts` 加 `err_ark_*` 文案） | P1 |
| 5 | 音色克隆效果验证 | `reference_audio`（data URI）是否真正生效需实测对比：同文稿有无音频参考各生成一次对比听感 | P1 |
| 6 | 参考视频接入或移除 | 表单收了参考视频（`video` 字段）但未提交给方舟（base64 体积限制）。要么压缩后接入，要么从表单删掉，不留「收了不用」的误导 | P2 |

关键文件：`src/lib/digitalhuman.ts`、`src/lib/ark.ts`、`src/app/api/digitalhumans/*`、`tools/dh-worker.mjs`、`src/components/DigitalHumanPanel.tsx`

## 颜新航 · 提示词生成模块

| # | 任务 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 方舟 LLM 接入（地基）+ 合规配套 | 用现有 `ARK_API_KEY` 调 doubao 对话模型（`POST /api/v3/chat/completions`）。**随第一项同步做**：生成文本过 `moderateText` 审核、`trackEvent` 埋点（建议事件名 `prompt_generated`）、配额策略（建议：提示词生成免费不计入 AI 配额，产生图片生成才计） | **P0** |
| 2 | AI 祭品提示词助手（核心价值） | 用户输入「白菊花」→ LLM 扩写「一束白菊，露水未晞，柔光侧照……」优质生图提示词。在祭品生成入口加「帮我写」按钮，可一键替换再生成 | **P0** |
| 3 | 提示词模板库 | 按祭品分类（鲜花/灯烛/食品/祭仪/物件）预置精选模板，一键套用后可改 | P1 |
| 4 | 数字人文案代写 | 根据生平简介自动生成数字人说话稿（≤500 字），接入 `DigitalHumanPanel` 的文稿输入区 | P1 |
| 5 | 墓志铭/生平润色助手 | 纪念馆编辑页（PATCH `/api/memorials/[id]` 前端）的文案辅助 | P2 |

关键文件：`src/lib/ark.ts`（可扩展或新建 `src/lib/llm.ts`）、`src/app/api/items/generate/route.ts`、`src/components/OfferPanel.tsx`、`src/components/DigitalHumanPanel.tsx`

## 刘贻清 · 资质合规 + 部署 + 统筹（账号级操作）

| # | 任务 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 深度合成算法备案 | 数字人正式运营硬性要求，周期约 1 个月。官方系统自办免费，代办 0.8~3 万。**一次把图片生成+数字人都写进算法说明** | **P0** |
| 2 | 微信开放平台资质 + SMTP | 300 元/年认证 → 扫码登录；邮箱验证码上线需配 `SMTP_URL`（免费额度够用） | P1 |
| 3 | 阿里云内容安全密钥 | 配 `ALIYUN_GREEN_*` 即启用自动审核，替换人工兜底 | P1 |
| 4 | 微信/支付宝支付 | 需营业执照（个体户可）+ 商户号；费率 0.38%~0.6%，无固定费 | P1 |
| 5 | 生产部署 | 服务器（轻量 2C4G 约 300~1000 元/年）+ 域名 + HTTPS + 进程守护；`tools/backup.mjs` 挂定时任务 | P1 |
| 6 | OSS 对象存储 | `StorageAdapter` 已预留，上量后从 local 切换（初期月费几十元） | P2 |
| 7 | 漏斗数据运营 | 埋点已上线（见 PRD 3.0 §6），每周看 `GET /api/admin` 的 `funnel` 数据，对照验收线（全链 ≥21%、周活跃馆 ≥50、回访 ≥25%） | 持续 |

## 公共任务池（2026-08-17 已分配）

| 任务 | 负责人 | 备注 |
|---|---|---|
| **E2E 测试补全**（T14 尾巴） | **刘贻清** | 最优先：三人并行推送的回归保护栏，与备案等待期互补 |
| **墓园性能**（F4.6）墓位分页/虚拟滚动 | **刘洋** | 数字人 P0 完成后的第二战线 |
| **V05 墓园场景润色 / V11 3D 增强** | **刘洋** | 与墓园性能同域，一次熟悉三处受益 |
| **V06 动效反馈 或 V07 移动端专项**（二选一） | **颜新航** | 提示词模块 P0 落地后再做，勿分散主力 |
| **V08 无障碍 / V09 视觉性能 / V10 全站视觉回归** | **刘贻清** | V10 与自建 visual-baseline 同源；V09 于 2026-08-17 认领 |

## 成本速查（年度）

| 项 | 费用 |
|---|---|
| 服务器 + 域名 + 证书 | ~400-1200 元/年 |
| 微信认证 | 300 元/年 |
| 内容安全 / OSS / SMTP | 月费合计 <100 元 |
| 备案（一次性） | 自办 0 / 代办 0.8~3 万 |
| 方舟 API（按量） | 生图每张几毛、视频每条几元 |
