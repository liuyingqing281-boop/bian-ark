# 彼岸 · PC 端前端设计文档

> 依据：`docs/前端具体设计流程.md`（移动端设计规范）与移动端高保真原型（`prototype/*.html` + `prototype/shared/ui.css`）派生。PC 端与移动端**同一份视觉语言、同一套信息架构、同一份文案边界**，仅做宽屏适配与效率增强。
> 本文是 PC 端前端实施的唯一界面要求来源；与 `docs/web/02-PC后端设计文档.md`、`docs/web/03-PC-API接口文档.md` 配套。
> 针对原型 `prototype/pc.html` 的布局整改意见见 §一；红线（文案/商业化/诚实原则）与移动端完全一致，不重复定义，违者打回。
> 2026-08-23 修订：视觉规范由「宣纸白浅色」改为**延续移动端「熔火/余烬」深色主题**，与线上移动端原型保持一致（以 `shared/ui.css` 为准）。

---

## 一、现有原型（pc.html）问题诊断与整改结论

| # | 问题 | 整改结论 |
|---|---|---|
| 1 | 视觉风格 | **延续移动端「熔火/余烬」深色主题**（§2.1）：深黑底 + 熔橙渐变 + 烛金点缀 + 发光/玻璃质感，与移动端原型 `shared/ui.css` 完全一致，PC 端不另造色彩体系 |
| 2 | 主内容区无最大宽度，宽屏（≥1600px）下卡片与文本行过宽 | 内容容器 `max-width: 1120px` 居中（§3.2） |
| 3 | AI 对话固定占右栏 380px，挤压主内容，违背「AI 不是主角」 | 改为**可收起侧板**（收起为右缘 56px 竖条，展开 360px 覆盖式），状态本地记忆（§4.4） |
| 4 | 左侧导航把想念/祭奠/记忆档案/亲友平铺为一级导航，与移动端「3 个一级入口」架构冲突 | 左栏一级只保留：纪念馆 / 发现（后续）/ 我的；想念·祭奠·记忆档案作为馆内的区块锚点与二级导航（§3.1） |
| 5 | 主区内部 5 列网格嵌套过深，卡片过窄 | 主区 12 列网格，内容 8 列 + 辅助栏 4 列，≥1440px 生效；<1440px 退化为单列（§3.3） |
| 6 | 主 CTA 全宽、按钮过多抢视觉 | PC 主 CTA 宽度收敛（§五），Hero 操作区分组排列 |

---

## 二、视觉规范（延续移动端熔火/余烬主题，仅列 PC 差异）

### 2.1 色彩与质感系统（以 `prototype/shared/ui.css` 为准，全端统一）

> **色板与全部视觉令牌以 [`设计令牌.md`](./设计令牌.md) 为单一事实来源（SSOT）**；本节仅为概览，色值/渐变/辉光/动效参数若不一致，以设计令牌文档为准。

| 角色 | 色值 | 用途与限制 |
|---|---|---|
| 背景 · 深黑 | `#070302`（`--bg`） | 全局底色，叠加熔橙径向氛围渐变（顶部/底部各一处，克制） |
| 主色 · 熔橙 | `#FF7A2F`（`--ember`） | 主 CTA、点灯/供奉动作、当前态标识 |
| 点缀 · 烛金 | `#FFB35C`（`--ember-soft`） | 强调文字、年份、链接、图标点缀 |
| 深橙 | `#B43A0E`（`--ember-deep`） | 渐变收尾、品牌块 |
| 成功 · 苔绿 | `#7FB069`（`--ok`） | 「免费」标、供奉完成 |
| 警示 · 余烬红 | `#E0604F`（`--danger`） | 删除等破坏性操作 |
| 文字 | 主 `#FFF6EC` / 次 `rgba(255,246,236,.6)` / 弱 `rgba(255,246,236,.38)` | 三级文字透明度 |
| 卡片 | `rgba(255,255,255,.05)` + 1px `rgba(255,255,255,.09)` 细边 | 玻璃卡片，配 backdrop blur |

- 质感：**发光球体、烛火、玻璃卡片、橙色辉光是本产品的既定视觉资产**，全端保留；辉光仅用于 Hero 照片、品牌块、供桌顶部氛围三处，不得扩散到普通内容卡片。
- 字体：标题衬线（Noto Serif SC / 宋体系），正文无衬线（PingFang / 雅黑系），与移动端一致。
- 圆角：卡片 12–22px（内容卡 12，Hero/弹层 22+），主按钮全圆角。

### 2.2 PC 差异项

| 项 | 移动端 | PC 端 |
|---|---|---|
| 字号阶梯 | 24 / 17 / 16 / 13 | 32(Hero 名) / 20(区块标题) / 14–15(正文) / 11–12(辅助)；行高仍 1.6 |
| 可点区域 | ≥44×44pt | ≥36×36px；主 CTA 高 48–52px，**不全宽**，宽度 ≤320px |
| 悬停态 | 无 | 所有可点元素必须有 hover 态（背景白 4% 或透明度 85%），过渡 150ms ease-out |
| 焦点态 | — | 键盘 Tab 焦点环：2px 烛金（`--ember-soft`）外描边，offset 2px（无障碍强制） |
| 动效 | 200–300ms | 同；侧板展开/收起 250ms ease-out；禁止弹跳 |
| 滚动条 | 隐藏 | 细滚动条 6px，thumb `rgba(255,255,255,.12)` 圆头 |

---

## 三、布局框架

### 3.1 总体结构

```
┌────────────────────────────────────────────────────────────┐
│ 左导航 224px（固定）│  主内容区（滚动，max 1120px 居中）  │ 对话侧板 │
│  · 品牌区            │                                    │ 收起:56px│
│  · 纪念馆            │   ┌─ Hero 沉浸区 ─────────────┐   │ 展开:360px│
│  · 发现（后续）       │   │ 照片 名字 生卒 寄语 CTA    │   │（覆盖式， │
│  · 我的              │   └──────────────────────────┘   │ 不挤压主区）│
│  · 底部用户卡         │   记忆 │ 想念 │ 祭奠（吸顶锚点）  │          │
│                    │   ┌ 8 列 ────────┐ ┌ 4 列 ──────┐ │          │
│                    │   │ TA 的人生      │ │ 留下你的话    │ │          │
│                    │   │ 祭奠供桌/宫格  │ │ 最近的纪念    │ │          │
│                    │   │              │ │ 记忆档案摘要  │ │          │
│                    │   └──────────────┘ └────────────┘ │          │
└────────────────────────────────────────────────────────────┘
```

- **左导航 224px**，`position: fixed`，深色半透明 + backdrop blur；内容项与移动端一级导航一致：纪念馆 / 发现（后续阶段，置灰）/ 我的；馆内区块（记忆/想念/祭奠/亲友）**不**进左导航。
- **主内容区**：`margin-left: 224px`，内部容器 `max-width: 1120px; margin: 0 auto; padding: 32px 40px`。
- **对话侧板**：`position: fixed; right: 0; top: 0; height: 100vh`。收起态为右缘 56px 竖条（烛金图标 + 「和 TA 说说话」竖排）；展开态 360px，深色毛玻璃**覆盖式**（悬浮于内容之上），不挤压主区宽度。首次使用仍必经身份说明弹层（§4.3，文案逐字同移动端 §2.3，不可删改）。

### 3.2 断点系统

| 断点 | 宽度 | 行为 |
|---|---|---|
| ≥1440px | 宽屏 | 12 列网格：内容 8 列 + 右辅助栏 4 列 |
| 1024–1439px | 常规桌面 | 主区退化为单列：TA 的人生 → 祭奠 → 留下你的话 → 最近的纪念 → 记忆档案摘要，顺序同移动端信息流 |
| 768–1023px | 小窗/平板横屏 | 左导航收起为 64px 图标栏；主区单列 |
| <768px | — | 提示「请使用手机访问」或跳转移动站，不做降级适配（V1 范围外） |

### 3.3 网格与间距

- 12 列网格，列间距 24px；卡片间距 24px；区块间距 32–40px。
- 卡片内边距 24px；Hero 区内边距 32px。
- 信息密度主动留白原则同移动端：一屏不追求塞满。

---

## 四、页面设计图纸（PC 适配版）

> 仅描述与移动端的**差异**；控件清单、文案、三态（空/加载/错误）定义逐条继承 `前端具体设计流程.md` §二，PC 端不得另造文案。

### 4.1 纪念馆首页 ★★★★★

- Hero 沉浸区：背景封面图 50% 透明度 + 左深右浅渐变遮罩；横向布局——左为 TA 照片（128px 圆形，**保留移动端的发光辉光** `0 0 44px rgba(255,122,47,.55)`），中为名字（衬线 32px）/ 生卒年 / 寄语 / 灯亮状态（小火焰 + 「灯还亮着」自然文案），右为操作列（主 CTA「和 TA 说说话」+ 分享/编辑/亲友三个次按钮宫格）。
- Tab 锚点条：吸顶（`top: 0`），深色 82% 透明度 + backdrop blur + 底部 1px 白 8% 分割线；当前 Tab 熔橙 2px 下划线 + 烛金文字。
- 「TA 的人生」时间线：左轴单列（年份烛金衬线 + 熔橙圆点 + 渐隐轴线），同移动端；事件 ≥6 条时可选左右交错，稀疏内容不强行交错。
- 祭奠区：供桌横幅（顶部熔橙径向氛围光 + 烛火动效保留）+ 6 项宫格一行排开（不再 3 列换行）；免费标苔绿、付费标 ¥X 烛金，价格点击前可见；「✨ 为 TA 准备特别的礼物」为通栏描边卡片，权重低于宫格。
- 右辅助栏（4 列）：依次「留下你的话」「最近的纪念」「记忆档案摘要」。「留下你的话」提交成功 toast 与列表刷新规则同移动端。

### 4.2 想念表达

PC 不单设想念页：「留下你的话」卡片常驻首页右辅助栏；「你留下的」历史列表以弹层或二级路由 `/miss` 呈现，布局为居中 640px 单列。

### 4.3 身份说明弹层（首次必经）

PC 为**居中弹层**（宽 480px，深色卡片 `#150806` + 细边，遮罩 55% 黑 + 轻 blur，**不可点遮罩关闭、无右上角 ×**，唯一出口是底部按钮），声明文案逐字同移动端 §2.3。确认后写入本地态并展开对话侧板。

### 4.4 对话侧板 ★★★★★

- 容器：360px 覆盖式，深色 92% 底 + backdrop blur + 左侧 1px 白 8% 边 + 左投影；250ms ease-out 滑入滑出。
- 顶栏：TA 小头像（带烛金辉光）+ 「和 + 称谓 + 说说话」+ ⋯（清空对话/反馈问题）+ 收起按钮；副标题「纪念性 AI · 不是 TA 本人」。
- 消息区：**沿用移动端气泡样式**——用户气泡右对齐熔橙渐变底白字；TA 气泡左对齐玻璃卡底 + 细边；气泡最大宽度为侧板宽 78%；「查看这句话的依据」11px 烛金下划线文字链（无依据不显示）；推测角标 11px 弱字；日期分隔线「── 今天 ──」弱字细线。
- 「回答依据」为侧板内弹层（不新开全局弹窗）。
- 补充记忆闭环：AI 不知道时气泡下方「添加一段关于 TA 的记忆」→ 侧板内表单 → 保存 toast「已保存到 TA 的记忆档案」→ 回到对话。
- 快捷 chip：空对话时横排在输入框上方，对话开始后隐藏。
- 输入框：底部固定玻璃胶囊（`inputbar`），占位「想对 TA 说……」，🎙 语音输入（按住说话、流式上屏，规格见 §十一；未配语音服务时回落置灰态「语音功能正在准备中」），Enter 发送 / Shift+Enter 换行。
- 状态：回复中三点呼吸动画 + 「正在想你问的话…」可中断；错误「刚才没说上话，再试一次」+【重试】；敏感内容「这个话题我们轻轻带过」。
- 埋点同移动端 §五。

### 4.5 记忆档案

- 二级路由 `/archive`，主区单列（max 760px 居中）：顶部「已建立 N 条记忆」+ 五分区卡片（👤/❤️/🎵/💬/📄），分区空状态文案逐字同移动端。
- 底部固定【＋ 添加记忆】改为页面右上主按钮；添加流程为居中弹层（选分区 → 录入 ≤500 字 → 保存 toast）。
- 编辑/删除：hover 条目右侧浮现操作图标；删除必经二次确认弹窗（余烬红标识）。
- 首页右栏「记忆档案摘要」展示 3 条 + 条数，点击进 `/archive`。

### 4.6 祭奠与 AI 生成纪念物

- 一口价确认弹窗：宽 400px 居中深色卡片，元素白名单（图标+名称+价格+说明+双按钮）逐字同移动端 §六-3，禁止倒计时/库存/攀比；遮罩 55% 黑，点遮罩=取消。
- 供奉成功：供桌即时刷新 + ≤1.2s 烛火温和动效，无音效（默认关闭）；点灯后 Hero 区「灯还亮着」自然文案。
- AI 生成纪念物为二级路由 `/gift` 三步流：PC 上三步为**横向步骤条 + 单卡片**（顶部 1→2→3 步骤指示，内容区单卡切换），第 3 步纪念物大图居中展示，按钮组【收藏到纪念馆】【分享给亲友】【再准备一件】。
- 「帮我写」每日限量提示、失败自动退费提示，文案同移动端。

### 4.7 我的

二级路由 `/me`：主区单列 max 760px——用户卡（头像/打码昵称/守护天数）、个人资料、我的纪念（创建/协作/纪念过三态聚合）、订单记录（一口价流水表格：时间/项目/金额/状态，支持分页）、设置（通知/隐私/注销）。

### 4.8 亲友共同纪念

二级路由 `/family`：馆主可生成/更换邀请链接（复制按钮 + 二维码可选）；成员列表打码展示；访客态与权限规则完全由服务端视图模型 `viewerRole` 驱动，前端不自行推断。

---

## 五、通用组件 PC 规格（差异项）

| 组件 | PC 规格 |
|---|---|
| 主按钮 | `btn-primary` 沿用熔橙渐变 + 全圆角；高 48–52px、**不全宽**（宽度随内容，≤320px）；默认/hover(透明度 85%)/置灰（透明度 35%） |
| 次按钮 | `btn-ghost` 玻璃底 + 细边；高 40–52px |
| 弹窗 | 宽 400–480px，深色卡片 `#150806` + 细边 + 圆角 16–26px，遮罩 55% 黑 + 轻 blur；单弹窗原则不变 |
| Toast | 顶部居中或右下（PC 底部常被对话侧板占用），深色半透明底白字 14px，停留 2s |
| Action Sheet | PC 改为**居中弹层或下拉菜单**，不再底部升起 |
| 骨架屏 | 白 5% 呼吸块（深色底上），>300ms 才显示；按卡片轮廓 1:1 占位 |
| 空状态 | 线性小图标 + 一句说明 + 一个引导按钮；文案同移动端 |
| 表格（订单等） | 行分割线白 5%，行高 48px，表头 12px 弱字 |
| 键盘可达 | 全站 Tab 顺序合理，Esc 关弹层/侧板（身份说明层除外），对话输入框 Enter 发送 |

---

## 六、路由与信息架构

```
/                     纪念馆首页（默认馆或馆选择器）
/archive              TA 的记忆档案
/miss                 你留下的（历史表达）
/gift                 为 TA 准备一份礼物（三步流）
/family               亲友共同纪念
/me                   我的（资料/纪念/订单/设置）
/discover             发现（后续阶段，占位）
对话侧板不占路由，全局悬浮组件（localStorage 记忆展开态）
```

- 多纪念馆切换：左导航「纪念馆」项内下拉切换，URL 携带 `?memorialId=`。

---

## 七、埋点（PC 端增量）

继承移动端 §五全部埋点，另增：

| 埋点 | 目的 |
|---|---|
| 侧板展开/收起 | PC 对话入口效率验证 |
| 首屏完成动作（看/说/做）耗时 | 「30 秒内完成其一」PC 验证 |
| 多馆切换次数 | 多馆用户识别 |

## 八、验收口径（PC 端增量）

1. 移动端 §六 全部条目沿用（文案边界、弹窗白名单、动效时长、三态截图）。
2. 三档断点（1440 / 1280 / 1024）截图走查：容器限宽、单列退化、导航收起逐项核对。
3. 键盘走查：Tab 焦点环全程可见，Esc 关闭所有弹层与侧板（身份说明层除外）。
4. 风格核对：与移动端原型同屏对比——色值（`--bg/--ember/--ember-soft`）、玻璃卡片、辉光位置（仅 Hero 照片/品牌块/供桌三处）逐项一致；不得出现浅色/白色页面。
5. 对话侧板收起态下，主内容区不得以任何形式为其预留宽度。

---

## 九、首页概念稿落地（2026-08-23）

概念稿 `docs/web/concept/index.html`（截图 `shot-1-candle.png` / `shot-2-dusk.png` / `shot-3-star.png`）已落地为**项目首页** `/[lang]`。

### 9.1 实现

- 组件：`src/app/[lang]/ConceptHome.tsx`（客户端组件），路由页 `src/app/[lang]/page.tsx` 仅做 lang 解析后渲染。
- 视觉：WebGL 折叠光绸着色器（fbm 域扭曲 + 正弦折叠），四组色板「烛夜 / 黄昏 / 晨雾 / 星夜」由右下角「换个意境」按钮逐帧插值切换；鼠标视差缓动跟随。
- 降级：无 WebGL 时移除 canvas，露出 CSS 静态径向渐变；`prefers-reduced-motion` 时光流速度降至 0.15。
- 性能：`devicePixelRatio` 上限 2；单全屏三角形 + rAF 循环，无外部依赖。
- 版式：在站点布局（NavBar 高 4rem）内全屏呈现，容器高 `calc(100vh - 4rem)`；主标题「彼岸」（宋体系字族）+ 标语「思念有处安放」+ CTA「进入彼岸」→ `/[lang]/garden`。英文语言下标题/标语/CTA 显示对应英文。
- 原首页的纪念馆列表内容（精选/最新/类型筛选）由 `/[lang]/garden` 承接，不再出现在首页；历史实现见 git 记录。

### 9.2 设计令牌映射

| 概念稿变量 | 值 | 用途 |
|---|---|---|
| `--ink` | `#10141c` | 页面底色 |
| `--gold` | `#d8a95c` | CTA 描边/文字、按钮 hover |
| `--text` | `#e8e2d6` | 主文字 |
| `--muted` | `rgba(232,226,214,.55)` | 标语/角标 |

色板着色参数（底色 + 两层流动光 + 微光，rgb 0-1）见组件内 `PALETTES` 常量，与概念稿一致。

### 9.3 登录页同款光绸视觉（2026-08-23 拍板）

- PC 登录页 `/[lang]/login`（`src/app/[lang]/login/page.tsx`）已采用「概念光绸背景 × 暗色登录卡」组合：`ConceptStage`（`showPalette=false`）+ 半透明毛玻璃卡片 `.login-card`（`rgba(16,20,28,.55)` + `backdrop-filter: blur(14px)` + 圆角 20px + 1px 暖白描边 `rgba(232,226,214,.14)`），标题「彼岸」/ 标语「思念有处安放」与首页同源。
- 手机端原型屏 01（登录注册屏）按同一方案落地：背景由纯暗暖渐变（`#070302` + 橙色辉光）替换为同款光绸着色器并保留「换个意境」，表单控件结构与接口契约不变；主 CTA 沿用烛火橙实心全宽按钮。移动端图纸见《前端具体设计流程》§2.0「视觉规范」；参考图 `docs/assets/15-login-bg-ink.png`（背景意境）/ `docs/assets/15-login-form.png`（表单布局）。

### 9.4 登录/注册分离（2026-08-24 拍板，✅ 已实施；2026-08-25 密码登录增量见 §9.5）

> 拍板：注册与登录彻底分开，废除「登录即注册」。**手机号 / 邮箱 / 微信三种方式平级**，同等地用于登录与注册；默认进登录，注册需专门点击切换。接口契约见《08》§3.0（verify/qrcode 均带 `intent`）；数据库侧仅 M11（`auth_oauth_states.intent`）。已落地 `components/LoginForm.tsx`（双 tab + 通道切换 + 协议勾选 + `?error=wechat_*` 引导）与 `/[lang]/login/page.tsx`（error 透传）；移动端同构图纸见《前端具体设计流程》§2.0。

**卡片结构（`.login-card` 顶部双 tab，下划线态，与移动端屏01 同构）**

```
┌─ 登录卡 ────────────────────────────────┐
│   [ 登录 ]   [ 注册 ]      ← 默认落「登录」；「注册」需专门点击
│─────────────────────────────────────────│
│ 登录 tab：                                │
│   ( 手机 | 邮箱 )           ← 通道切换，两通道平级          │
│   [ 手机号/邮箱输入 ]                      │
│   [ 验证码 ] [ 获取验证码 60s ]            │
│   【 进入彼岸 】            ← verify intent=login │
│   【 微信扫码登录 】         ← 次按钮，qrcode intent=login │
│   先看看，不登录             ← 访客链                      │
│ 注册 tab（专门点击后展示）：                │
│   ( 手机 | 邮箱 )           ← 通道切换，与登录 tab 同构平级  │
│   [ 手机号/邮箱输入 ]                      │
│   [ 验证码 ] [ 获取验证码 60s ]            │
│   [ 怎么称呼你（昵称，选填）]              │
│   ☑ 我已阅读并同意《用户协议》《隐私政策》   ← 必勾才可提交/发起微信 │
│   【 注册并进入 】           ← verify intent=register │
│   【 微信注册 】             ← 次按钮，qrcode intent=register │
└──────────────────────────────────────────┘
```

**交互细则**

| 控件 | 行为 |
|---|---|
| tab 切换 | 默认「登录」；切 tab 清空表单；视觉沿用 §9.3 卡片，tab 用与通道切换同款下划线态 |
| 登录·手机/邮箱通道 | `request-code(channel)` → `verify(intent=login)`，两通道平级；`404 account_not_found` → toast「该手机号/邮箱还未注册」+ 卡内按钮「去注册 →」自动切注册 tab（保留输入） |
| 注册·手机/邮箱通道 | `request-code(channel)` → `verify(intent=register, name?, agreed)`，两通道平级；`409 already_registered` → toast「该手机号/邮箱已注册，可直接登录」+ 切回登录 tab（保留输入） |
| 注册·协议勾选 | 未勾选时【注册并进入】【微信注册】置灰；后端兜底 `400 agreement_required` |
| 微信登录/注册 | `POST /api/auth/wechat/qrcode {intent}` 取 `{url,state}` 整页跳转授权；回跳错误经 `/{lang}/login?error=wechat_not_registered｜wechat_already_registered` 落地，登录页据此 toast + 自动切对应 tab；成功落地 `/zh/me` |
| 访客链 | 「先看看，不登录」行为不变（只读公开馆，动作再引导回登录） |

- 埋点：`login` / `register` 由接口侧打点（《08》§四），前端不重复上报。
- 红线（与移动端 §2.0 一致）：不出现「注册领礼包」等诱导话术；不强制登录看公开馆；协议未勾选禁提交。

### 9.5 账号密码登录与忘记密码（2026-08-25 拍板，✅ 当日已实施）

> 拍板：**注册即设密码**——注册 tab 新增「密码 / 确认密码」两行（👁 显隐切换），注册成功后**手机号/邮箱即本产品账号**，下次直接账号+密码登录；登录 tab 在验证码方式之外新增「密码」方式（两式平级切换，验证码方式保留不动，用户可继续验证码登录）；新增「忘记密码」三步流。接口契约见《08》§3.0（verify 注册带 `password` + `login-password` / `reset-password`），数据库侧仅 M12（`users.password_hash`，《09》§B12/B14）。**已落地**：`components/LoginForm.tsx`（方式切换 + 密码行 + 👁 + 忘记密码三步浮层 + `password_not_set` 双引导链）与移动端原型 `views/auth.html` / `auth.js` / `shared/api.js` 同构改造完成；词典 `dictionaries.ts` 中英双语补齐文案。实现差异：注册成功为内联成功条「注册成功 · 账号与密码请妥善保管」+ 1s 后跳转（PC 无全局 toast）；原型为 toast。

**密码规则（前后端同规，单点工具函数复用）**：长度 8–64；大写字母 / 小写字母 / 数字 / 特殊符号四类须含**至少 3 类**。密码行下方弱字提示规则文案并实时判定；不满足或两次不一致时【注册并进入】/【重置密码】置灰；后端兜底 `400 weak_password`（不核销验证码，修正后同码重交）。

**卡片结构（§9.4 双 tab 不变；登录 tab 内加「方式切换」行，注册 tab 插入密码两行）**

```
┌─ 登录卡 ──────────────────────────────────────┐
│   [ 登录 ]   [ 注册 ]          ← 双 tab 不变，默认「登录」
│───────────────────────────────────────────────│
│ 登录 tab · 方式切换：( 密码 | 验证码 )  ← 下划线态，默认「密码」│
│                                               │
│ ▸ 密码方式（新）：                              │
│   [ 手机号 / 邮箱 ]             ← 单账号框，前端按格式自动识别通道 │
│   [ 密码                👁 ]    ← 默认密文；👁 切换显隐 │
│                        忘记密码？ ← 右对齐文字链 → 三步浮层 │
│   【 进入彼岸 】                ← POST login-password │
│   【 微信扫码登录 】  先看看，不登录              │
│                                               │
│ ▸ 验证码方式（= §9.4 现行结构，零改动）：         │
│   ( 手机 | 邮箱 ) + [账号] + [验证码][获取 60s]   │
│   【 进入彼岸 】 / 微信扫码登录 / 先看看          │
│                                               │
│ 注册 tab（密码两行插在验证码与昵称之间）：         │
│   ( 手机 | 邮箱 )                              │
│   [ 手机号 / 邮箱 ]                            │
│   [ 验证码 ] [ 获取验证码 60s ]                 │
│   [ 密码                👁 ]                   │
│   [ 确认密码            👁 ]    ← 与上行不一致即行内报错 │
│   [ 怎么称呼你（昵称，选填）]                    │
│   ☑ 我已阅读并同意《用户协议》《隐私政策》         │
│   【 注册并进入 】 / 【 微信注册 】               │
└───────────────────────────────────────────────┘
```

**忘记密码浮层（卡内三步切换，URL 不变、纯前端态；遮罩同 `.login-card` 毛玻璃）**

```
步骤① 找回账号                     步骤② 验证并重置                    步骤③ 完成
[ 手机号 / 邮箱 ]                  [ 6 位验证码 ] [获取 60s]           ✓ 密码已重置
【 发送验证码 】                    [ 新密码              👁 ]          请用新密码登录
 ← 返回登录                        [ 确认新密码          👁 ]          【 去登录 】
                                   【 重置密码 】
```

**👁 显隐切换（需求明确的双态图标）**：密码默认密文（`type="password"`），此时输入框尾部图标 = **划了斜线的眼睛**（表示当前不可见）；点击切换为明文（`type="text"`），图标 = **眼睛**；纯前端态不落库。注册两处、忘记密码两处、登录一处共五个密码行同款控件。

**交互细则**

| 控件 | 行为 |
|---|---|
| 方式切换（密码/验证码） | 默认「密码」；切换清空登录 tab 输入（tab 位置不变）；「验证码」= §9.4 现行行为零改动（需求：可继续使用验证码登录） |
| 密码方式·账号框 | 单输入框，前端按格式自动识别手机号/邮箱并置 `channel`；`404 account_not_found` → toast「该手机号/邮箱还未注册」+「去注册 →」切注册 tab（保留账号输入） |
| 密码方式·密码框 | `401 invalid_credentials` → 行内「账号或密码不对，再看看」；`401 password_not_set`（微信注册/历史验证码账号）→ toast「该账号未设置密码」+ 文字链「验证码登录」「忘记密码」；`429 too_many_attempts` → 「错得太频繁，15 分钟后再试」 |
| 忘记密码 | 三步：① `request-code` 发码（60s 倒计时，同款限频）→ ② `reset-password`（`400 invalid_code` → 「验证码错误」；`weak_password` → 规则提示同码重交）→ ③ 成功态【去登录】切回登录 tab·密码方式并回填账号；`404 account_not_found` → toast「该手机号/邮箱还未注册」+【去注册】 |
| 注册·密码两行 | 两行均 👁 显隐；确认行与首行不一致 → 行内「两次输入的密码不一致」，一致且合规前【注册并进入】置灰；规则不满足 → 行内规则提示；`400 weak_password` 后端兜底（不核销验证码） |
| 注册成功 | toast「注册成功」→ 自动登录进纪念馆首页（现行落点不变）；卡片下方一次性弱字提示「账号与密码请妥善保管」（不落库、不重复弹） |
| 微信方式 | 密码/验证码两种方式下均展示【微信扫码登录】次按钮（不变）；微信注册且未绑手机/邮箱的账号无「账号」可用，不弹密码引导，继续微信扫码登录 |

**按键排布规范（需求 7「排布好看」的落地口径）**

- 层级自上而下固定四段：`tab 行 → 方式/通道切换行 → 输入组（含行内文字链）→ 动作区`；动作区内部固定 `主 CTA（全宽 52px 烛火橙实心，全卡唯一强焦点）→ 次按钮（微信，同宽描边态弱一级）→ 文字链（先看看，居中垫底）`。
- 「忘记密码？」右对齐紧贴密码行下缘（与输入组绑定，不进动作区）；步骤浮层内主次同理（发送/重置/去登录为主，「← 返回登录」为左上文字链）。
- 行间距与 §9.3 卡片节奏一致（输入组 12px、动作区与输入组间 20px）；方式切换、通道切换、tab 三级均用同款下划线态，视觉一套语法不新增控件样式。

- 埋点：`login`（密码方式）/ `reset_password` 由接口侧打点（《08》§四），前端不重复上报。
- 红线：密码明文不进 URL / 日志 / localStorage；无「注册领礼包」话术；协议未勾选禁提交（§9.4 延续）。

---

## 十、纪念园「星海」与馆内「长明灯阵」（2026-08-23 新增）

> 交互与视觉规格的单一事实来源：`docs/四模型分工/墓园页面前端设计规格.md` §8（星海）与 `docs/13-馆内多人合馆（长明灯阵）展示方案.md`（灯阵）。本节仅定义 PC 端差异项；场景色彩沿用 §2.1 熔火/余烬体系（深黑底 + 熔橙 + 烛金），星光/灯焰即既有发光资产的复用。

### 10.1 园—馆—人连续缩放（PC 差异）

| 交互 | PC 端行为 |
|---|---|
| 推近/拉远 | 滚轮缩放（以光标为中心）+ 双击推近；缩放档位与 2.5D/3D 分段控件（墓园规格 §2）共存 |
| 聚焦星群/灯 | 单击聚焦（400–700ms），hover 预亮 + 名牌浮出（移动端无 hover，对应长按预览） |
| 园→馆过渡 | 推近过阈值后无缝切入 `/[lang]/hall/[馆id]` 灯阵场景；拉远返回 `/[lang]/garden` 并恢复镜头与浏览状态 |
| 择位/摆位 | 鼠标拖拽，进入「布阵/择位模式」才可拖（防与场景平移冲突）；Esc 退出模式 |
| 键盘 | 方向键在星群/灯间移动焦点，Enter 聚焦，Esc 层级回退；焦点环 2px 烛金（§2.2） |

### 10.2 PC 布局落点

- 星海页（`/garden`）与馆内灯阵（`/hall/[id]` 多人馆）均为**无框场景全宽铺满**，浮层规则（导航 `z-20`、抽屉 `z-30`、提示 `z-40`）与墓园规格 §2 一致；不与 §3.1 的左导航布局混用——场景页隐藏左导航，仅保留顶部悬浮导航（返回/搜索/视图切换）。
- 抽屉在 PC 宽屏为**底部横向卡片轨道**（2–6 张，墓园规格 §3）；聚焦详情态为底部居中限宽抽屉（max 880px），双主操作并列。
- 馆级公共层（群像名单 + 全馆留言墙 +「为全家点灯」）在 ≥1440px 时可为右侧 4 列辅助栏呈现，<1440px 回到底部抽屉。
- 性能：LOD 合并光晕、星域分片加载、可见区域虚拟化（墓园规格 §8.5）；`devicePixelRatio` 上限 2 与 §九首页一致；`prefers-reduced-motion` 下缩放连续体降级为瞬时切换。

### 10.3 红线与文案

- 星海/灯阵任何位置不出现访问量、热度、榜单；名人星域仅「略亮」，不配特殊文案。
- 择位引导文案：「为 TA 选一个安放的位置」；合祭按钮文案：「为全家点灯」；均遵守 FR-08 调性规范，禁用营销话术。

---

## 十一、对话语音交互（2026-08-23 新增，FR-13/14）

> 规格的单一事实来源：`docs/14-语音能力方案.md`；接口契约见 08 §3.14。语音**只出现在对话侧板**（§4.4）与角色创建/设置页，其余页面不做。

### 11.1 🎙 语音输入（对话侧板输入框）

- 🎙 位于输入框右侧、发送按钮左侧；常态 40% 透明度降为常态可点（hover 全亮，焦点环 2px 烛金）。
- 按住说话：按下即录（AudioWorklet 直出 WAV），输入框上方浮出 48px 录音条——烛金呼吸点 + 已录时长 + 「松开发送识别」；60s 上限前 5s 轻提示，到时自动结束。
- 松开/结束后识别文字**流式填入输入框**（光标处追加，不打断已输入内容），用户确认/修改后走既有发送链路。
- 识别失败：录音条位显示「没听清，再说一次？」+【重试】；`503` 降级 Web Speech API，浏览器不支持则 🎙 回置灰态（文案「语音功能正在准备中」）。

### 11.2 🔊 朗读（TA 气泡）

- 🔊 11px 弱图标置于 TA 气泡左下角（与「推测」角标同行），点击流式播放，播放中变 ⏸；**永不自动播放**，无提示音。
- 复刻音色（F9 `mode=clone` 且 approved）播放时，气泡右下角加 11px 弱字「AI 合成声音」。
- 降级 SpeechSynthesis 时无角标变化之外的差异；浏览器不支持则 🔊 隐藏。

### 11.3 角色创建「声音」配置（建馆向导第 2 步子项 + 馆设置页）

- 入口文案：「TA 的声音（选填）」；不出现「克隆/AI 训练」字样（红线同向导分区命名）。
- **A 档**：预置音色卡片横排试听（播放键 + 音色名）或「描述生成」输入框（≤100 字，示例「年迈女性，语速慢，带江南口音」）；试听文案固定三句任选（如「慢慢来，不着急。」）；确认后 toast「声音已保存」。
- **B 档**：「用 TA 生前的声音」→ 上传样本（mp3/wav ≤10MB）+ 强制勾选「我是逝者近亲属或已获合法授权」（不勾选提交键置灰）→ 提交后状态卡「声音审核中」（驳回显示原因 +【重新上传】）。
- 试听走 `POST /api/voice/preview`；保存走 `POST /api/memorials/[id]/voice`（08 §3.14）；未保存的试听零落库。

### 11.4 PC 差异与红线

- PC 按住说话为鼠标按住（触屏设备同移动端手势）；录音中 Esc 取消。
- 红线：不营造「实时语音通话」错觉（无全双工、无来电式 UI）；朗读与语音输入是两个独立辅助按钮；无自动播放、无声音诱导分享（FR-08）。
