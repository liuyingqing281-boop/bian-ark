# 部署运维日志

## 2026-08-19

- 已补充统一支付 provider、订单幂等和 mock smoke。
- 恢复演练命令：`node tools/restore-check.mjs <backup.db>`。
- 运行前先用 `npm run db:backup` 生成 SQLite 备份；恢复检查会复制到临时目录并执行完整性检查、外键检查和迁移计数，不修改原备份。
- 实际演练：备份 `data/backups/bian-2026-08-19T06-42-10-235Z.db`；`integrity_check=ok`、外键错误 `0`、迁移数 `10`，恢复检查通过。
