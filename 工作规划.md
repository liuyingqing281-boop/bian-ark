# 彼岸 · 线上墓园 — 前端界面优化工作规划（v2）

> 生成时间：2026-08-11 ｜ 开发环境：Next.js 16 / React 19 / Tailwind 4 / TS 7，dev 端口 3002
> 技能库：E:\skills库\Final_Product_Library（14,904 条）、E:\skills库\skills（find-skill）、E:\skills库\mattpocock-skills

---

## 一、项目现状快照

### 已完成代码（8/10-8/11 落盘）
| 文件 | 状态 | 说明 |
|------|------|------|
| `src/components/NavBar.tsx` | ✅ 已建 | 客户端组件，`usePathname()` 按路径高亮导航 |
| `src/components/MemorialHero.tsx` | ✅ 已建 | 页头半透明渐变 + 头像/封面可点击上传 |
| `src/components/Flame.tsx` | ✅ 已建 | Canvas 粒子火焰动画 |
| `src/components/OfferPanel.tsx` | ✅ 已改 | 祭品卡片改为渐变写实样式 |
| `src/app/[lang]/layout.tsx` | ✅ 已改 | 接入 NavBar / ThemeBackground |
| `src/app/[lang]/memorial/[id]/page.tsx` | ✅ 已改 | 接入 MemorialHero + Flame |
| `src/app/[lang]/dictionaries.ts` | ⚠️ 乱码 | 中文文本为 mojibake，且字符串字面量未闭合 → 触发首页 500 |

### 关键阻塞问题（必须先修）
- **`dictionaries.ts` 中文乱码**：整份中文文案被多重编码破坏（如 `褰煎哺 路 绾夸笂澧撳洯`），部分字符串未闭合（`...,?`），导致模块解析失败 → `localhost:3002` 首页返回 **500**。
- **修复原则**：禁止用 PowerShell 直接重写该文件（会再次破坏编码）；用 Node.js/Python 以 UTF-8 读改写，或逐一补全字符串闭合。

---

## 二、任务拆分与技能映射

### 任务 0（前置）：修复 dictionaries.ts 乱码 → 恢复服务
- **问题**：中文文案乱码 + 字符串未闭合，首页 500，所有页面无法访问。
- **方案**：用脚本以 UTF-8 重写中文文案（zh 部分），保留 en 部分；校验 `tsc --noEmit` 通过。
- **涉及文件**：`src/app/[lang]/dictionaries.ts`
- **技能映射**：
  - `FED-4058` React（组件/字典结构规范）
  - `FED-4055` UI文案与微文案指南（中文文案质量）
  - `FED-0329` Web应用测试（修复后验证 200）

### 任务 1：场景色彩被遮挡 → 完全显示四季场景
- **问题**：原 memorial 页头 `from-stone-700 to-stone-900` 不透明灰底遮挡 ThemeBackground 场景；`body` 为 `bg-stone-950` 不透明。
- **当前进度**：MemorialHero 已用半透明 `from-stone-700/70 to-stone-900/80`，无封面时场景可透出。
- **剩余工作**：验证 `-z-10` 场景是否被 body 背景遮挡；必要时将 body 背景改为透明或提升场景层级，确保夏季/秋季主题色完整显示。
- **涉及文件**：`src/components/MemorialHero.tsx`、`src/components/ThemeBackground.tsx`、`src/app/[lang]/layout.tsx`
- **技能映射**：
  - `FED-0278` frontend-design（高品质界面设计）
  - `FED-0326` Frontend Design（Anthropic 设计规范）
  - `FED-1131` 前端UI/UX（设计师视角布局/层级）
  - `browser:control-in-app-browser`（视觉验证主题切换）

### 任务 2：导航栏高亮错误 → 正确高亮当前页
- **问题**：原导航始终高亮"我的"，其他页面无反馈。
- **当前进度**：NavBar 用 `usePathname()` 实现：`/zh` 高亮首页，其余匹配 `pathname === href || startsWith(href+"/")`；"我的/登录"按 user 状态切换。
- **剩余工作**：浏览器点验首页/花园/会员/管理/我的 各页高亮；确认 `/me`、`/garden` 等子路径覆盖。
- **涉及文件**：`src/components/NavBar.tsx`、`src/app/[lang]/layout.tsx`
- **技能映射**：
  - `FED-0122` ui-ux-pro-max（导航交互/视觉反馈）
  - `FED-0085` frontend-patterns（导航/路由模式）
  - `FED-7876` 交互设计（激活态动效过渡）
  - `browser:control-in-app-browser`（逐页验证）

### 任务 3：头像与背景上传 → 页头直接可点上传
- **问题**：头像/背景只能在"我的"页设置。
- **当前进度**：MemorialHero 已实现 owner 上传：头像 hover 覆盖层 + 右上角封面上传按钮，走 `POST /api/upload` → `PATCH /api/memorials/{id}`。
- **剩余工作**：验证未登录隐藏上传、上传后刷新、`cover_url` 以 `/uploads/` 开头才渲染背景的边界条件。
- **涉及文件**：`src/components/MemorialHero.tsx`、`src/app/api/upload/route.ts`、`src/app/api/memorials/[id]/route.ts`
- **技能映射**：
  - `FED-0131` frontend-enhancer（上传交互增强）
  - `FED-4055` UI微文案（上传中/成功/失败提示）
  - `FED-0329` Web应用测试（上传流程端到端）
  - `browser:control-in-app-browser`（模拟上传验证）

### 任务 4：祭品写实风格 → 替换纯 emoji
- **问题**：官方祭品为 emoji 图标，缺乏真实感。
- **当前进度**：OfferPanel 的 ItemCard 已用基于 `item.id` 的渐变写实底色 + 阴影；AI 生成走 `imagegen`（`STYLE_SUFFIX="写实摄影风格"`）。
- **剩余工作**：可进一步为常见祭品（菊花/香/蜡烛/元宝）预置写实 SVG 精灵或本地图，替换纯 emoji；确认 AI 生成图片为写实摄影风。
- **涉及文件**：`src/components/OfferPanel.tsx`、`src/lib/imagegen.ts`
- **技能映射**：
  - `FED-0278` frontend-design（写实视觉设计）
  - `imagegen`（已安装，生成写实祭品图）
  - `FED-0324` Canvas设计（写实 SVG/视觉资源）
  - `FED-1131` 前端UI/UX（卡片质感/光影）

### 任务 5：仿真火焰效果 → 燃烧祭品真实火焰
- **问题**：`is_burning=1` 仅显示"已烧"标签，无火焰。
- **当前进度**：Flame 组件已建（Canvas 粒子：暖色粒子 + 发光 + 底部辉光），已集成到祭品墙燃烧条目（图/图标旁）。
- **剩余工作**：优化火焰视觉（锥形、摇曳、明暗闪烁）、性能（粒子上限、`devicePixelRatio` 钳制）、确认多燃烧条目不卡顿。
- **涉及文件**：`src/components/Flame.tsx`、`src/app/[lang]/memorial/[id]/page.tsx`
- **技能映射**：
  - `FED-1103` threejs-animation（粒子/动画参考）
  - `FED-1111` threejs-shaders（发光/辉光着色参考）
  - `FED-7876` 交互设计（微交互质感）
  - `browser:control-in-app-browser`（性能与视觉验证）

---

## 三、执行顺序与进度时间线

| 阶段 | 任务 | 状态 | 依赖 |
|------|------|------|------|
| P0 | 修复 dictionaries.ts 乱码 → 恢复 200 | ⏳ 待执行 | 无（阻塞项） |
| P1 | 任务 2 导航高亮 | ✅ 代码 / 待验证 | 无 |
| P2 | 任务 1 场景色彩 + 任务 3 上传 | ✅ 代码 / 待验证 | P0 |
| P3 | 任务 4 祭品写实 | ✅ 基础 / 可增强 | P0 |
| P4 | 任务 5 火焰效果 | ✅ 基础 / 可优化 | P0 |
| P5 | 端到端浏览器验收 + 文档 | ⏳ 待执行 | P0-P4 |

## 四、验证清单（浏览器 localhost:3002）
- [ ] `/zh` 首页 200，场景夏季/秋季色完整，导航高亮"花园"
- [ ] `/zh/memorial/<id>` 页头背景/头像上传可用，燃烧祭品有火焰
- [ ] `/zh/me` 导航高亮"我的"
- [ ] 右下角主题选择器切换夏季/秋季/夜晚，场景可见
- [ ] 中/英切换后文案无乱码

## 五、技能速查
- 检索：`E:\skills库\skills\find-skill\SKILL.md`
- 前端大包：`E:\skills库\Final_Product_Library\02_全栈大前端包\`
- React 开发：`FED-4058`；前端设计：`FED-0278`/`FED-0326`；UI/UX：`FED-0122`/`FED-1131`；交互动效：`FED-7876`；Web 测试：`FED-0329`；Three/Canvas：`FED-1103`/`FED-1111`/`FED-0324`
